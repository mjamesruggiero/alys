# Copy to `.env` and fill in. `.env` is gitignored.
# Last.fm API credentials — get a key at https://www.last.fm/api/account/create
LASTFM_API_KEY=
LASTFM_API_SECRET=

# Local path to the raw Spotify extended streaming history export.
SPOTIFY_RAW_DIR=~/Desktop/debris/spotify_data
